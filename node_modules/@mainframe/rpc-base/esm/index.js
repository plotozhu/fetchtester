import _createClass from "@babel/runtime/helpers/esm/createClass";
import _defineProperty from "@babel/runtime/helpers/esm/defineProperty";
import nanoid from 'nanoid';

var BaseRPC =
/*#__PURE__*/
function () {
  function BaseRPC(canSubscribe) {
    if (canSubscribe === void 0) {
      canSubscribe = false;
    }

    _defineProperty(this, "_canSubscribe", void 0);

    this._canSubscribe = canSubscribe;
  }

  var _proto = BaseRPC.prototype;

  _proto.createId = function createId() {
    return nanoid();
  };

  _createClass(BaseRPC, [{
    key: "canSubscribe",
    get: function get() {
      return this._canSubscribe === true;
    }
  }]);

  return BaseRPC;
}();

export { BaseRPC as default };